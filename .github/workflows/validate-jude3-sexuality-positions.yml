name: Validate Jude 3 sexuality positions

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate-jude3-sexuality:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Validate Jude 3 sexuality positions
        run: python3 scripts/validate-jude3-sexuality-positions.py .
